{
  "name": "themearr-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "End-to-end smoke tests. Deliberately a separate package from src/Themearr.Web so the release workflow's `npm ci` never installs Playwright or downloads a browser.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
