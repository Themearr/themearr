<!-- Thanks for contributing! See CONTRIBUTING.md for the full guide. -->

## What & why

<!-- What does this PR change, and why? Link the related issue if there is one, e.g. "Closes #123". -->

## How was it tested?

<!-- Tests added/updated, manual verification steps, screenshots for UI changes. -->

## Checklist

- [ ] `dotnet test` passes
- [ ] `npm test`, `npm run lint`, and `npx tsc --noEmit` pass in `src/Themearr.Web` (if the frontend changed)
- [ ] Commit prefix matches the intended version bump (`feat:` → minor, otherwise patch — see CONTRIBUTING.md)

<!-- Heads up: merging to main auto-releases when app code changed. Docs-only changes don't cut a release. -->
